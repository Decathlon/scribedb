loglevel: INFO
source:
  db:
    type: postgres
    host: localhost
    port: 5432
    username: postgres
    password: fddfdfdf
    dbname: postgres
    qry: select first_name from employee order by first_name;
  oracle:
    host: localhost
    port: 1521
    username: system
    password: Decathlon0147
    instance: XE
    qry: select first_name from employee order by first_name;
target:
  oracle:
    host: localhost
    port: 1521
    username: system
    password: Decathlon0147
    instance: XE
    qry: select first_name from employee order by first_name;
